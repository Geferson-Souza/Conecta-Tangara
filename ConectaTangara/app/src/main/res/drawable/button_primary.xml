<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Quando pressionado -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/berkeley_blue" /> <!-- Vinho escuro da paleta -->
            <corners android:radius="24dp" />
        </shape>
    </item>
    <!-- Estado normal -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/cerulean" /> <!-- Azul petróleo da paleta -->
            <corners android:radius="24dp" />
        </shape>
    </item>
</selector>
